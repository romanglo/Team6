/**
 * 'logger' package contains an singleton implementation to logger, which log to
 * the console and to external file.
 * <p>
 * 'Common' project contains interfaces and classes which are shared both to the
 * server and to the client.
 */

package logger;
