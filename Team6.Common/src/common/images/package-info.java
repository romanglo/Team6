/**
 * 'common.images' package contains the images which used by the classes in
 * 'common' package.
 * <p>
 * 'Common' project contains interfaces and classes which are shared both to the
 * server and to the client.
 */

package common.images;
